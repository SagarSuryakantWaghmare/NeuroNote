# Production Environment Variables
# Backend API URL for production (your deployed backend)
VITE_BACKEND_URL=https://your-neuronote-api.vercel.app

# API Proxy Target (not used in production, but included for completeness)
VITE_API_PROXY_TARGET=https://your-neuronote-api.vercel.app

# Environment
VITE_NODE_ENV=production

# App Configuration
VITE_APP_NAME=NeuroNote
VITE_APP_VERSION=1.0.0
